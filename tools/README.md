# 工具目录

该目录只放可复用的开发与现场辅助工具，不放产品业务逻辑。产品模块不得反向依赖 `tools/`。

- `benchmark/`：采集、处理、写盘、传输和 UI 刷新的可重复基准。
- `diagnostics/`：只读检查或明确受控的链路、文件、设备诊断。
- `migration/`：旧 `.rcscan`、参考文件或基准数据的显式迁移工具。

任何可能改写数据的工具必须默认 dry-run，打印输入、输出和 schema 版本，并保留原文件。
