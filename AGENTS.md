# UAV-GPR 项目长期规则

本文件是本仓库所有开发人员和编码代理必须遵守的最高级项目规则。每次执行任务前必须完整阅读本文件，再按 [docs/INDEX.md](docs/INDEX.md) 阅读相关专题文档。

## 1. 项目目标与当前状态

本项目开发一套适配无人机的探地雷达软件，由地面端和空中端组成：

- **地面端是主应用**：任务配置、连接管理、校准、处理、B-scan、GNSS 地图、主存档、回放、导出和诊断集中在地面端。
- **空中端是轻量执行器**：只负责 LibreVNA/GNSS 接入、执行冻结任务、本地可靠存储、状态上报和数据回传；UI 仅显示必要状态与安全操作。
- **空地均保存原始数据副本**：空中端先可靠落盘再发送；地面端校验后增量保存并确认。

当前仓库仅完成结构和文档初始化。任何未由代码和测试证明的能力都必须表述为“计划”或“未实现”。

## 2. 参考项目的使用边界

### 2.1 主要实现参考

`E:\钢筋仪软件开发` 是以下部分的权威参考：

- `src` 分层与不可变核心模型；
- LibreVNA USB 传输、协议组包、S 参数采集和暂停/恢复；
- 模拟采集与文件回放；
- OSL 校准、空采参考、`.rcal/.rcbg`；
- 带通、IFFT、Dewow、Flat Reflection 等独立处理阶段；
- `.rcscan` HDF5 的总体数据组织和处理历史；
- PySide6 + pyqtgraph 的 B-scan-first UI 思路。

迁移前必须记录参考文件的路径和内容哈希。迁移是“提取契约并适配”，不是复制整个窗口或巨型模块。

### 2.2 受限参考

`E:\UVA_GPR_system` 只允许作为以下内容的参考：

- GGA/RMC 等 NMEA 解析思路；
- GNSS 独立读取、重连、最近 fix 缓存、过期判断和与 sweep 时刻匹配；
- HM30 网络拓扑、IP、供电、尺寸和现场干扰测试等需要再次核实的部署事实。

禁止直接迁移该项目的：

- 空中端或地面端主窗口；
- `RemoteSender`、接收线程和现有 NPZ 线上协议；
- 每道 CSV/NPZ 正式存储；
- 校准、处理和 B-scan 实现；
- Matplotlib 实时绘图；
- 依赖在线 CDN/瓦片的 Leaflet 地图；
- legacy/continuous 双采集路径。

如果两个参考项目冲突，除 GNSS 专题外默认以钢筋仪项目的契约为准；仍有疑问时必须通过 ADR 决策。

## 3. 强制数据规则

- 原始频域数据 `frequency_raw` 永远不可修改、覆盖或原地处理。
- 数据模型从第一天支持多通道；禁止先写单通道特殊结构再补多通道。
- 单道频域形状固定为 `channel × frequency`。
- 连续频域形状固定为 `trace × channel × frequency`。
- 时域连续数据形状固定为 `trace × channel × time`。
- 所有数组轴、单位、通道和 S 参数绑定必须显式保存。
- 深度模型未标定前默认显示时间轴，不得把时间轴伪装为深度轴。
- 每个处理阶段输入不可变、输出新对象，并追加可序列化的参数、版本和处理历史。
- `frequency_raw`、可选 `frequency_calibrated`、`time_base`、可选 `time_processed` 分开保存。
- OSL 校准、空采背景、频域带通、IFFT、Dewow、Flat Reflection、零时和连续背景是不同阶段，不得混为一个黑盒函数。
- `.rcscan/.rcal/.rcbg` 必须包含格式名和 schema 版本；不兼容版本必须拒绝或显式迁移。

## 4. 标识、时间和一致性规则

- 每次正式任务生成新的 `mission_id`；不得以网络连接周期代替任务周期。
- 每一道具有任务内单调 `trace_index` 和全局唯一 `trace_uid`。
- `trace_uid`、`mission_id`、`trace_index` 在空地两端和传输协议中保持一致。
- 每一道保存 UTC 采集时间、单调时钟时间、目标间隔、实际间隔和调度误差。
- 每一道原始数据计算规范化 `raw_trace_sha256`；空地一致性以逐道原始哈希为准，不能用两个完整 HDF5 文件的哈希代替。
- 已存在相同 `mission_id + trace_index` 但哈希不同的数据属于冲突：必须 fail-closed、报警并保留证据，禁止覆盖后 ACK。
- 任务结束状态必须区分完成、用户停止、故障停止和崩溃恢复。

## 5. GNSS 强制规则

- GNSS 读取与雷达采集解耦，使用独立线程或异步边界；串口读取不得阻塞采集或 UI。
- 雷达道与 GNSS fix 以 sweep 中点时刻匹配，保存匹配误差和 fix 年龄。
- 没有有效定位时位置必须为空并带原因；禁止复用过期 fix 冒充当前实测位置。
- `latitude/longitude`、MSL 高程、fix 类型、卫星数、HDOP、地速、航向、NMEA UTC、接收 UTC、有效性和数据年龄分别建模。
- MSL、椭球高和相对地面 AGL 不得混用；没有 AGL 来源时不得推算或标注为 AGL。
- 可选原始 NMEA 必须明确隐私和体积策略。

## 6. 空地职责与可靠存储

### 空中端

- 连接 LibreVNA、GNSS 和地面链路；
- 校验并冻结地面端下发的任务配置；
- 使用单一生产采集路径执行任务；
- 完整 sweep 先增量写入本地 `.partial.rcscan` 并 flush；
- 生成逐道 UID/哈希，进入持久化 outbox，再尝试发送；
- 断网不停止采集，磁盘不可写或数据不完整则安全停止任务；
- 只提供连接、设备、GNSS、任务、存储、待同步数量和停止等最小状态 UI。

### 地面端

- 创建任务、配置参数、加载校准、下发命令并显示明确执行结果；
- 接收规范原始频域数据和元数据，校验、去重、增量保存和 ACK；
- 完成校准/处理、实时 B-scan、GNSS 地图、回放、导出和一致性检查；
- 不把地面端显示是否成功作为空中端数据保存成功的依据。

### 可靠性

- 固定采用 store-then-forward；内存队列不能成为唯一待发记录。
- ACK 只表示地面端已验证并持久化，不表示“收到了一些字节”。
- 断线重连后通过 manifest 对账和缺道请求补传。
- 空中端数据至少保留到地面端完整验证；清理必须显式、可审计。

## 7. 线程、性能和 UI 规则

- USB、串口、网络、HDF5 写盘、解压/解码和长时处理不得运行在 UI 主线程。
- UI 只消费有界快照或信号，不持有硬件和传输细节。
- 队列必须有上限、背压策略和可观测指标；禁止无界内存队列。
- 实时 B-scan 使用 pyqtgraph，并复用图元；禁止每次刷新重建 figure、axes、image 或 colorbar。
- 不得在每次刷新时对完整历史反复 `column_stack` 或全量重算；使用追加缓冲、环形视图或受控批处理。
- 主窗口中央只展示数据：左侧上下双 B-scan，右侧 GNSS 地图；设置、校准和诊断进入顶部菜单或对话框。
- 地图默认支持离线或无底图降级，不依赖在线 CDN；长轨迹必须抽稀或分批更新。
- B-scan 与地图按 `trace_uid` 双向联动，无 GNSS 的道也必须可见且明确标记。
- 主要目标分辨率是 1440×900 和 1920×1080，最低验收 1280×720，并支持 Windows DPI 缩放。

## 8. 时窗与采集间隔

- “物理无模糊时窗”和“显示/处理裁剪时窗”是两个独立概念。
- 物理时窗由频率步进等扫频参数决定，改变它需要重新校验/配置采集。
- 显示时窗只在物理时窗范围内裁剪，可由地面端在回放和显示时调整，不修改原始数据。
- 采集间隔由地面端配置、空中端用单调时钟执行；保存目标值、实际值和调度误差。
- 配置在任务开始后冻结；修改关键参数必须开始新任务，不能在同一任务中静默改变数据契约。

## 9. 包依赖规则

允许的总体依赖方向：

```text
ui -> application -> domain services -> core
apps -> ui/application
acquisition/calibration/processing/storage/positioning/transport -> core
application -> acquisition/calibration/processing/storage/positioning/transport
core -> Python standard library + numpy only（如确有需要）
```

- `core` 不依赖 Qt、硬件、文件格式或网络。
- 算法层不依赖 UI。
- 存储层不依赖 UI，也不调用网络。
- transport 不导入具体窗口；协议模型与 socket/链路实现分离。
- 地面端和空中端编排可共享领域模型，但不得互相导入应用实现。
- `tools/` 可依赖产品包，产品包不得依赖 `tools/`。

## 10. 测试和工作方式

每个任务开始前：

1. 阅读本文件和相关专题文档；
2. 检查工作树和参考基线；
3. 写明范围、排除项和验收条件；
4. 先测试契约或建立可失败的回归测试，再实现；
5. 运行相关测试、全量非硬件测试和静态检查；
6. 检查 diff、状态和生成物；
7. 更新受影响文档和 ADR；
8. 报告实际完成内容、测试证据、未完成项和风险。

强制测试规则：

- 每项能力必须覆盖正常、错误、取消/恢复路径。
- 硬件代码必须有模拟器或协议夹具测试；真实硬件测试单独标记。
- 存储和协议必须有版本契约、重复消息、乱序、截断、冲突和崩溃恢复测试。
- UI 测试验证状态和线程边界，不用 `sleep` 猜时序。
- 禁止删除测试、降低断言或吞掉异常来解决失败。

## 11. Git 与数据安全

- `main` 禁止直接开发；每个明确任务使用独立分支。
- 不修改任务范围外的用户文件；不覆盖脏工作树。
- 禁止 `git reset --hard`、强制推送和大范围未核实删除。
- 不提交实测数据、地图缓存、日志、密钥、本地配置、`.vendor`、虚拟环境和构建产物。
- 对数据文件的修复默认输出新文件并保留原件；正式清理需要显式授权。

## 12. 文档权威顺序

发生冲突时按以下顺序处理：

1. 当前任务的明确验收条件；
2. 本 `AGENTS.md`；
3. 已接受的 `docs/adr/` 决策；
4. 对应专题设计文档；
5. 参考项目代码和文档。

任何会改变强制数据规则、空地职责或持久化语义的方案都必须先写 ADR，不能只在代码中形成事实。
