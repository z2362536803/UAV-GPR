# 参与开发

## 开始之前

1. 完整阅读 `AGENTS.md` 和与任务相关的设计文档。
2. 确认任务边界、验收条件和明确排除项。
3. 检查工作树，不覆盖与任务无关的本地修改。
4. 一个任务只解决一个清晰问题；发现额外问题先记录，不顺手扩大范围。

## 分支与提交

- `main` 只接收经过验收的变更，不直接开发。
- 每个任务使用独立分支，建议命名为 `feat/<issue>-<slug>`、`fix/<issue>-<slug>` 或 `docs/<issue>-<slug>`。
- 提交前必须检查差异、运行适当测试并确认没有实测数据、缓存、密钥和本地配置。
- 禁止通过删除测试、放宽数据校验或吞掉异常来让测试通过。
- 禁止强制推送和破坏性重置。

## 完成定义

一个功能任务只有在以下条件全部满足时才算完成：

- 代码位于正确层级，没有越层依赖或把阻塞操作放进 UI 主线程。
- 正常路径、错误路径和恢复路径有测试。
- 模型、配置、协议或文件格式变更同步更新文档和版本信息。
- 新硬件能力具有模拟器或回放测试，硬件测试单独标记。
- 数据完整性失败采用显式报错或 fail-closed 行为。
- 相关测试、静态检查和格式检查通过。

## 文档规则

- 已批准的架构改变必须新增或更新 ADR。
- 尚未实现的设计必须明确写“计划”或“未实现”，不得描述成现状。
- 命令、字段、单位、数组形状和状态名称必须精确，不使用含糊同义词。
- 外部硬件参数必须注明来源和复核日期；现场验证结果不得与厂商标称值混写。
